javascript:var millisec = 300;var startTop = document.documentElement.scrollTop || window.pageYOffset || document.body.scrollTop;			var interval = 20;			var pixelPerInterval = interval*startTop/millisec;			var scrollInterval = setInterval (function(){				window.scrollBy(0,-pixelPerInterval);startTop-=pixelPerInterval;				if(startTop<=0){					clearInterval(scrollInterval);				}			},interval);